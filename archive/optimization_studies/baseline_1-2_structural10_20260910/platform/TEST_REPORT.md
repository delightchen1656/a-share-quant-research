# 静态交付检查

- Python语法：通过。
- SuperMind禁用调用扫描：不包含 `import os`、`open(`、`getattr(`。
- 主模型阈值：代码初始化时固定为0.63。
- 到期规则：自然日30天；浮盈达到10%则最长自然日60天。
- 原模型与先止损模型：继续以内嵌便携树形式加载。
- 状态：仅完成本地静态检查，仍需在SuperMind服务器实际回测并导出成交记录核对。
